# Шпаргалка: Команды Git для публикации

## 🚀 Быстрый старт (скопируй и выполни по порядку)

```bash
# 1. Инициализация Git
git init

# 2. Добавить все файлы
git add .

# 3. Проверить, что .env НЕ добавлен
git status

# 4. Первый коммит
git commit -m "Initial commit: AI Browser Agent"

# 5. Подключить GitHub репозиторий (замени YOUR_USERNAME!)
git remote add origin https://github.com/YOUR_USERNAME/ai-browser-agent.git

# 6. Переименовать ветку в main
git branch -M main

# 7. Загрузить на GitHub
git push -u origin main
```

## ✅ Готово!

Твой проект теперь на GitHub: `https://github.com/YOUR_USERNAME/ai-browser-agent`

## 🔄 Для будущих обновлений

```bash
git add .
git commit -m "Описание изменений"
git push
```

## 📝 Перед созданием репозитория на GitHub:

1. Перейди на https://github.com/new
2. Repository name: `ai-browser-agent`
3. Description: `Автономный AI-агент для управления веб-браузером`
4. Public/Private - выбери
5. **НЕ добавляй** README, .gitignore, лицензию
6. Создай репозиторий
7. Скопируй URL и используй в команде `git remote add origin`
